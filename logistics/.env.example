# PocketBase instance shared with the Xperience Delivery store.
# Veloxa only ever calls its public tracking / receipt / quote routes.
NEXT_PUBLIC_PB_URL=http://localhost:8090

# Canonical public URL of this site (used for metadata and share links).
NEXT_PUBLIC_SITE_URL=http://localhost:3001
